#include <bits/stdc++.h>
using namespace std;
    int n;
int main(){
    cin >> n;
    vector<int> a(n,0); //크기가 n인 벡터가 생성되고 모두 0으로 초기화됨.
    for(int i:a){
        cout << i <<" ";
    }

    return 0;
}